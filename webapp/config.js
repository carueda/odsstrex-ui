var odsstrexConfig = {

    // odsstres-rest endpoint URL.
    // Note that odsstrex-rest is CORS-enabled. The default value here simply
    // assumes that odsstrex-rest runs on the same host:port.
    rest: "/odsstrex-rest"
};

